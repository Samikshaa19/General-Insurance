import { RenewPolicyDetails } from './renew-policy-details';

describe('RenewPolicyDetails', () => {
  it('should create an instance', () => {
    expect(new RenewPolicyDetails()).toBeTruthy();
  });
});
